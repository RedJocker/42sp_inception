-- Create a database
CREATE DATABASE IF NOT EXISTS ${WORDPRESS_DATABASE_NAME};

-- Create a user and grant privileges
CREATE USER IF NOT EXISTS 'root'@'%' IDENTIFIED BY '1234';
GRANT ALL PRIVILEGES ON ${WORDPRESS_DATABASE_NAME}.* TO 'root'@'%';
FLUSH PRIVILEGES;
